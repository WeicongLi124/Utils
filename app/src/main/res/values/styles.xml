<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">#FF0000</item>
    </style>

    <style name="Dialog" parent="android:Theme.Dialog">
        <!--设置dialog背景色为透明-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowContentOverlay">@null</item>
        <!--设置遮罩层数值-->
        <item name="android:backgroundDimAmount">0.2</item>
    </style>

    <!--周报弹框选择器属性-->
    <style name="WeeklyRadioButtonStyle">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">#333333</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:drawablePadding">6dp</item>
        <item name="android:button">@null</item>
        <item name="android:drawableEnd">@drawable/weekly_radio_btn_selector</item>
        <item name="android:background">@android:color/transparent</item>
    </style>

    <style name="WeeklyDialogAnim">
        <item name="android:windowEnterAnimation">@anim/weekly_dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/weekly_dialog_exit_anim</item>
    </style>

</resources>
